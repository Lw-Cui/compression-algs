INCLUDE_DIRECTORIES(${COMPRESSION_ALGS_INCLUDE_DIRS})

ADD_EXECUTABLE(interface interface.cpp)
TARGET_LINK_LIBRARIES(interface ${COMPRESSION_ALGS_LIBRARIES})

ADD_EXECUTABLE(RecvServer server.cpp)
TARGET_LINK_LIBRARIES(RecvServer ${COMPRESSION_ALGS_LIBRARIES} pthread)